## 1. 缩放计算基础

- [x] 1.1 在图表预览模块中集中实现基于 viewport 可用尺寸、图形内在尺寸和安全边距的适屏比例计算，并移除固定 `1.5` 自动放大上限
- [x] 1.2 实现相对于适屏比例的动态最小/最大缩放边界（最大值至少为 `max(5, fit × 4)`），并提供统一 clamp 逻辑
- [x] 1.3 调整 inline SVG 尺寸测量优先级，使用有效 width/height、viewBox 或 getBBox 获取稳定内在尺寸，并对无效尺寸安全回退

## 2. 预览交互接入

- [x] 2.1 将 Mermaid inline 与 Kroki img 的首次加载路径接入统一的适屏和动态边界计算
- [x] 2.2 更新放大按钮、缩小按钮和滚轮处理，使其共享动态边界且均可从适屏状态继续有效放大
- [x] 2.3 监听实际预览 viewport 尺寸变化，更新适屏基准，并按设计区分适屏状态与用户主动缩放状态
- [x] 2.4 更新重置操作，使其使用最新适屏比例、清除偏移并恢复居中

## 3. 回归验证

- [x] 3.1 运行前端类型检查/生产构建，确认预览缩放改动不引入编译错误
- [ ] 3.2 在浏览器中验证小尺寸 Mermaid 与 Kroki SVG 初次预览能填充可用视口，并可继续放大至至少适屏比例四倍
- [ ] 3.3 在浏览器中验证大尺寸、超宽、超高图表的完整适屏、安全边距、按钮/滚轮边界和拖拽行为
- [ ] 3.4 调整预览视口尺寸，验证适屏状态自动更新、用户缩放状态被保留且重置返回最新适屏比例
