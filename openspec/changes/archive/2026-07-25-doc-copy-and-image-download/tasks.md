## 1. 核对文档复制源码

- [x] 1.1 检查 `DocumentToolbar` 是否已具备「复制源码」按钮、成功反馈与 `content` 为空时的禁用逻辑；缺口则补齐
- [x] 1.2 确认 Markdown / HTML、普通视图 / 全屏视图下工具栏均传入 `content` 且复制行为一致
- [x] 1.3 确认「下载文档」入口仍在且行为未改（与复制并存）

## 2. 图片预览下载

- [x] 2.1 抽取或新增前端下载工具（`fetch` → Blob → `<a download>`，含文件名解析与跨域失败回退）
- [x] 2.2 在 `ImagePreviewDialog` 工具栏增加「下载图片」按钮并接入上述逻辑
- [x] 2.3 确认 MarkdownViewer 与 HtmlViewer 共用该预览组件，无需各自重复实现

## 3. 验证

- [x] 3.1 手动验证：复制 Markdown/HTML 源码成功，有反馈；无内容时不可用
- [x] 3.2 手动验证：下载文档仍可用，文件名正确
- [x] 3.3 手动验证：本地 `/api/asset` 图片与外链图片均可从预览下载（或外链按设计回退）
- [x] 3.4 确认图表工具栏的复制/下载 SVG 未受影响
