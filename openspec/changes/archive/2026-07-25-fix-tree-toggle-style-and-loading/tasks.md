## 1. 侧栏边缘按钮样式统一

- [x] 1.1 在 `App.tsx` 抽出桌面端侧栏边缘按钮的共用样式（关闭按钮基准：`w-4 h-11`、图标 `h-3.5 w-3.5`、`opacity-40 hover:opacity-100 hover:w-5`）
- [x] 1.2 将文件树关闭按钮、文件树收起后展开按钮、大纲关闭/展开按钮全部改为共用样式，去掉文件树展开按钮的 `w-5 h-14` / `opacity-80` / 更大图标特例
- [x] 1.3 目视确认左右侧栏展开与关闭按钮尺寸一致

## 2. 服务端文件树缓存

- [x] 2.1 在 docs/server 层增加线程安全的内存树缓存（持有 `[]FileInfo` 或等价结构），提供 `Get` / `Rebuild`（或 `Invalidate+Rebuild`）
- [x] 2.2 服务启动时构建缓存；构建失败时记录错误且后续请求可回退同步 `ListTree`
- [x] 2.3 将 watcher 的树变更回调改为 debounce 后重建缓存，再建完后广播 `tree_reload`
- [x] 2.4 无参 `GET /api/files` 改为读缓存返回全量树；补充/更新相关 Go 测试

## 3. 按需读取 API

- [x] 3.1 扩展 `ListTree`/`scanDirectory`（或缓存切片）支持 `path` + `depth`，`depth=1` 只返回直接子节点
- [x] 3.2 `handleGetFiles` 解析可选查询参数；无参行为保持全量兼容
- [x] 3.3 为浅层根列表、子目录列表、非法 path 编写测试

## 4. 前端按需加载

- [x] 4.1 调整 `FileContext`：根层以 `depth=1` 加载；提供按目录 path 拉取子节点并合并进树状态的方法
- [x] 4.2 改造 `FileTree`：展开目录时触发子节点加载；展示加载中/失败态；`expandAll` 在缓存全量可用时一次拉全树，否则受控并发拉取
- [x] 4.3 深链/`currentFile` 变化时预取父路径链各层，保证展开与「定位」可用
- [x] 4.4 `tree_reload` 时失效按需查询并按当前展开集重拉，避免陈旧子树

## 5. 验证

- [x] 5.1 小文档库：行为与改前一致（树内容、选中、定位、展开/折叠）
- [x] 5.2 大文档库：首屏明显快于全量拉取；展开目录可在合理时间内出子节点
- [x] 5.3 增删文件后 WebSocket 刷新正确；HTML/ignore 规则未回归
