# 系统说明

构建一个实时 Markdown Files 在线渲染工具。

# 功能

1. 通过 `mdserve serve path/to/docs` 命令启动
2. 启动后，服务过滤指定目录下的 markdown 文件，前端显示按文件路径组织的结构
3. 可以通过浏览器访问服务查看组织结构，可点击文件渲染 markdown 文件
4. 服务可以指定监听端口、host(127.0.0.1/0.0.0.0/...)

# 技术要求

1. 前端

- typescript + react + vite + saas

2. 后端

需要选择合适的技术框架。
