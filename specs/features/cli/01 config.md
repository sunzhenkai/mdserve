# 描述

支持配置，注意配置是可选项。

1. 可以命令行参数指定配置文件
2. 默认读取 .mdserve.yaml

# 配置项

1. 网站名称
2. 默认展示的文档，如果不配置，则展示根目录的 README.md，忽略大小写
3. 配置文档地址，命令行配置覆盖配置文件配置
4. 服务相关配置，监听地址、端口
5. 配置自动拉取时间，支持配置分支，如果不是 git 库但配置了拉取则在启动时打印 warn 日志，但不要退出
